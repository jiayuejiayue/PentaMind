# PentaMind 项目现状梳理与功能扩展计划

> 更新时间：2026-03-04

---

## 一、项目当前架构概览

### 技术栈
| 层次 | 技术 |
|---|---|
| 前端 | Vue 3 (setup script) + Element Plus + Vite |
| 后端 | Spring Boot 3.2 + MyBatis-Plus + Java 21 |
| 数据库 | MySQL |
| AI 服务 | 恒脑 DAS AI 大模型 API |
| 浏览器自动化 | Java Playwright (无头 Chromium) |
| 安全工具编排 | YAML 描述符 + ToolManager 热加载 |

---

## 二、已实现功能清单

### ✅ 后端（已有 Controller/Service/Entity）

| 模块 | 实现内容 | 状态 |
|---|---|---|
| **目标管理** (`TargetController`) | CRUD + 分页查询 + **批量删除** | ✅ 完整 |
| **扫描任务** (`ScanTaskController`) | 任务创建、状态查询、任务列表 | ✅ 基础可用 |
| **端口数据** (`PortController`) | Nmap 端口结果的存储与查询 | ✅ 数据层完整 |
| **子域名** (`SubdomainController`) | 子域名数据存储与查询 | ✅ 数据层完整 |
| **路径数据** (`PathController`) | 目录扫描结果存储与查询 | ✅ 数据层完整 |
| **仪表盘** (`DashboardController`) | 统计总览 + 近期扫描 + **已装载工具列表** | ✅ 完整 |
| **插件系统** (`PluginController`) | 插件注册、心跳、上报（浏览器插件对接） | ✅ 基础完整 |
| **AI 全面分析** (`AnalysisController`) | 调用 Playwright 爬取页面 + 调用 LLM 分析 | ⚠️ 已接通，但 LLM 推断结果不稳定 |
| **工具编排引擎** (`ToolManager`) | YAML 热加载 + 工具执行 + 命令行参数拼装 | ✅ 核心完整 |
| **浏览器代理** (`PlaywrightAuthAgent`) | 无头浏览器启动、页面渲染、内容抓取 | ✅ 可用 |
| **恒脑 AI 服务** (`DasAiService`) | 多轮对话、Agent 调用、结果解析 | ⚠️ 有时解析格式失败 |

### ✅ 前端（已有 Vue 页面）

| 菜单路径 | 页面 | 状态 |
|---|---|---|
| 仪表盘 / 数据统计 | 统计卡片 + 扫描活动 + **已装载工具列表** | ✅ |
| 信息收集 / 目标管理 | CRUD + **批量删除** | ✅ |
| 信息收集 / 子域名 | 子域名数据展示 | ✅ 展示 |
| 信息收集 / 端口 | 端口数据展示 | ✅ 展示 |
| 信息收集 / 资产 | 资产列表 | ✅ 展示 |
| 功能点检测 / 页面扫描等 | 四个检测子页面 | ⚠️ 多为空壳 |
| 漏洞扫描 / 任务、路径等 | 扫描任务管理 | ⚠️ 基础框架 |
| 测试管理 / 用例、报告 | 测试管理页面 | ⚠️ 多为空壳 |
| 系统配置 / 策略/载荷等 | 系统配置页面 | ⚠️ 多为空壳 |
| 综合分析 | 分析页面（已连接 AI ） | ⚠️ 不稳定 |

### ✅ 工具集成（YAML 热加载方式）

> **配置方式**：`backend/tools/*.yaml` 文件在 Spring Boot 启动时由 `ToolManager` 自动扫描加载，动态注册到内存字典中，通过 `/api/dashboard/tools` 暴露给前端展示，同时供 LLM Function Calling 选参使用。**不硬编码**工具调用逻辑，完全通过 YAML 描述驱动。

| 工具名 | 用途 | 文件 |
|---|---|---|
| `nmap_port_scan` | 端口 & 服务指纹扫描 | `nmap.yaml` |
| `subfinder_domain` | 子域名被动收集 | `recon_tools.yaml` |
| `whatweb_fingerprint` | Web 技术栈指纹识别 | `recon_tools.yaml` |
| `httpx_alive_detect` | HTTP 存活探测 | `recon_tools.yaml` |
| `gobuster_dir` | 目录路径爆破 | `recon_tools.yaml` |
| `nuclei_vuln_scan` | PoC 模板漏洞扫描 | `recon_tools.yaml` |

---

## 三、当前核心痛点与不足

| 问题 | 说明 |
|---|---|
| **工具只是"配置了"，没有"跑起来"** | ToolManager 有 executeTool 方法，但扫描流程中实际没有串联 YAML 工具去执行 |
| **LLM 推断结果不能稳定写回数据库** | DasAiService 对 AI 的响应格式解析脆弱，任何格式变化都会导致静默失败 |
| **Playwright 工作，但结果没有结构化** | 页面爬取结果直接扔给 LLM，没有提取为结构化的功能点数据存入 `pm_function_point` 表 |
| **前端大量页面是空壳** | 功能点检测、测试用例、报告、系统策略等页面均无实际数据 |
| **没有漏洞利用闭环** | 现有自动化流程止步于"发现潜在问题"，没有实际 Payload 投递、无 PoC 验证 |
| **缺乏任务状态流转机制** | 扫描任务没有 Running→Completed 的可靠状态回调驱动 |
| **没有报告输出** | 目前没有任何 PDF/Markdown 形式的渗透报告生成 |

---

## 四、待实现功能（按优先级排序）

### 🔴 P0 — 优先打通最核心的"扫描→结果→展示"闭环

- [ ] **将 YAML 工具与扫描任务真正关联**：发起 Nmap 扫描 → ToolManager.executeTool → 结果写入数据库 → 前端展示 Port 列表
- [ ] **修复 DasAiService 的格式解析**：增加更宽容的 JSON 解析逻辑，不再因模型格式变化而静默失败
- [ ] **扫描任务生命周期管理**：PENDING → RUNNING → COMPLETED/FAILED，配合前端任务状态刷新

### 🟠 P1 — 建立功能点识别层（Phase 2 核心）

- [ ] **新增 `pm_function_point` 数据库表**：字段包含 URL、类型（登录/上传/搜索/API）、提取的参数列表
- [ ] **Playwright 结构化解析**：把爬取结果从"纯文本给 LLM"改造为"先结构化提取 `<form>`、AJAX 接口、输入参数，再存库"
- [ ] **功能点检测前端展示**：在"功能点检测"菜单下渲染识别出的表单、接口、输入点

### 🟡 P2 — 场景化漏洞测试

- [ ] **SQLi 自动检测模块**：对带参数的功能点批量投递 SQLMap 探针，结果由 LLM 二次研判
- [ ] **XSS 自动检测模块**：对输入框投递 XSS Payload，监控回显
- [ ] **目录爆破自动化**：集成 Gobuster/Dirsearch，发现目标后自动触发
- [ ] **子域名自动枚举**：发起 Subfinder 任务，结果写入子域名表

### 🟢 P3 — 报告与智能化

- [ ] **渗透报告自动生成**：将所有发现整合为 Markdown/HTML 报告，要求每条漏洞附带 PoC 请求
- [ ] **LLM 误报过滤**：对扫描结果二次投递 LLM 判定是否为真实漏洞
- [ ] **用户管理**：子账号划分，每个账号独立的任务和数据空间

---

## 五、可参考/补充的工具清单

> 这些工具添加只需在 `backend/tools/` 中追加 YAML 条目即可挂载

| 工具 | 用途 | 添加优先级 |
|---|---|---|
| `sqlmap` | SQL 注入自动化检测 | 🔴 高 |
| `ffuf` | 高性能路径/参数 Fuzz | 🔴 高 |
| `dirsearch` | Python 写的目录爆破，规则库更丰富 | 🟠 中 |
| `amass` | 深度子域名枚举（比 subfinder 更全） | 🟠 中 |
| `nikto` | Web 服务器已知漏洞快速扫描 | 🟠 中 |
| `xsstrike` | XSS 自动化检测与 Payload 绕过 | 🟡 低 |
| `feroxbuster` | Rust 实现的高速目录扫描 | 🟡 低 |
| `wappalyzer-cli` | 命令行指纹识别（更轻量） | 🟡 低 |

---

## 六、架构改进方向（参考 Shannon/CyberStrikeAI）

| 改进点 | 描述 | 参考来源 |
|---|---|---|
| **多 Agent 并行架构** | 将不同漏洞类型的检测解耦为独立 Agent 并行执行，而不是单线程串行 | Shannon |
| **PoC-First 策略** | 每个漏洞发现必须附带可复现的 curl 命令 / Playwright 脚本，否则不计入报告 | Shannon |
| **Skills 知识库** | 在项目中建立 `brain/skills/` 目录，存放 SQLi bypass、文件上传绕过等红队经验 Markdown，作为 LLM Prompt 动态注入 | CyberStrikeAI |
| **MCP 协议输出** | 将平台核心扫描能力包装为 MCP Server，可被 Cursor/Claude Desktop 直接调用 | CyberStrikeAI |
| **数据流分析 (DFA)** | 扫描源码追踪用户输入从 Source 到 Sink 的传播链，定位 SQLi/SSRF 的真实代码位置 | Shannon Pro |
| **大结果分页压缩** | Nmap/目录扫描返回数据可能超过 LLM 上下文限制，需要先落盘再让 LLM 分页读取摘要 | CyberStrikeAI |

---

## 七、核心原则（始终坚守）

1. **工具不硬编码** — 全部通过 `tools/*.yaml` + ToolManager 管理，新增工具只需加 YAML 文件
2. **LLM 是决策层，不是执行层** — LLM 选择用哪个工具、分析结果，但不负责执行 CLI 命令
3. **Shadow Crawler 是差异化壁垒** — 带登录态的浏览器录制能力是 Shannon/CyberStrikeAI 都没有的，不要轻易放弃这条线
4. **先打通闭环，再完善细节** — 优先做出一个从"输入目标"到"看到扫描结果"的完整链路，再考虑功能完善

---

*距离 "Zero-Touch Pentesting" 核心目标，当前完成度约 **20%**。基础架构已就绪，最关键的缺口是：YAML 工具真正被调用执行、结果被结构化存储、LLM 研判结果可靠写回、报告可输出。*

---

## 八、MCP + 多LLM 分层智能体改造计划

> 目标架构：**LLM 主脑自主规划 → MCP 工具调用 → Playwright 行为模拟 → 全自动渗透闭环**

### 阶段一：基础设施建设（预计 2~3 周）

**1. 搭建 Python MCP Server（独立进程）**
- [ ] 创建 `platform/mcp-server/` 目录，引入 Python `mcp` SDK
- [ ] 封装 dirsearch、nmap 为 `@mcp.tool()` 装饰器函数
- [ ] 通过 stdio 模式与 Java 后端联调，验证工具调用链路
- [ ] 后续工具（subfinder、nuclei、sqlmap、ffuf）逐步补充，每个只需加一个函数

**2. Java 端 MCP Client 集成**
- [ ] 引入 Spring AI 或自写轻量 HTTP Client 对接 MCP Server
- [ ] 替换 `ToolManager.executeTool()` 底层调用为 MCP 协议调用
- [ ] 保留 YAML 文件作为工具元数据文档，执行层迁移到 Python

**3. 多 LLM 分层配置中心**
- [ ] 新建 `LLMRouter.java`，维护多个 LLM 客户端
  - **主脑**：GLM-4 / DeepSeek-V3（高推理，统筹规划与最终报告）
  - **解读层**：GLM-3 / Qwen-Turbo（低成本，分析工具原始输出）
  - **本地层**：Ollama 接入（离线/私密任务）
- [ ] `application.yml` 中统一配置所有模型的 API Key 和 endpoint

**4. SSE 实时进度推流**
- [ ] 新增 `/api/agent/stream/{taskId}` SSE 接口
- [ ] 在 Agent 每个阶段开始/结束时推送节点状态事件

**5. 前端节点化进度面板**
- [ ] 新增「智能渗透」功能页（非对话式）
- [ ] 实现阶段节点流程图（参考 CI/CD pipeline 展示，可用 Vue Flow 实现）
- [ ] 每个节点可展开显示实时日志和发现条数

---

### 阶段二：Agent 调度核心（预计 3~4 周）

**6. 任务预配置弹窗**
- [ ] 目标输入 + 勾选执行哪些阶段（浏览器分析 / 工具扫描 / 功能渗透）
- [ ] 可选配置测试账号密码（供自动登录深度测试用）
- [ ] 工具选项勾选（子域名 / 端口 / 目录 / 指纹 / Nuclei）

**7. AgentOrchestrator.java（替换 ScanTaskServiceImpl）**
- [ ] 实现多阶段状态机：INIT → BROWSER_ANALYSIS → TOOL_SCAN → PATH_PROBE → FUNC_ANALYSIS → PENTEST → REPORTING
- [ ] 每阶段结束将结构化结果存库，并推送 SSE 进度事件
- [ ] 阶段失败不整体中断，记录失败原因后继续后续阶段

**8. 阶段① 深度浏览器分析（Playwright）**
- [ ] 将 Playwright 改造为独立 Python 微服务（FastAPI + Playwright）
- [ ] 分析页面：收集表单、输入框、AJAX 接口、按钮功能点
- [ ] 检测登录入口 → 如有账号密码则自动登录，保存会话 Cookies
- [ ] 结果以结构化 JSON 返回给 AgentOrchestrator，写入 `pm_function_point` 表

**9. 阶段② 工具扫描（MCP 并行调度）**
- [ ] AgentOrchestrator 将工具调用请求分发给 MCP Server
- [ ] 多工具（nmap + subfinder + dirsearch）通过线程池并行运行
- [ ] 每个工具结果由**解读层小模型**提炼为结构化 JSON 后返回给主脑
- [ ] 结果实时通过 SSE 推送到前端对应的功能栏

**10. 阶段③ 敏感路径深度窗探**
- [ ] 对目录扫描发现的 200/301 路径逐条 GET 请求
- [ ] LLM 分析响应内容：提取注释泄露、配置信息、隐藏参数、源码路径

---

### 阶段三：深度渗透与报告（预计 4~6 周）

**11. 阶段④ 功能点语义分析**
- [ ] 主脑 LLM 读取 `pm_function_point` 中所有功能点
- [ ] 对每个功能点判断：登录表单 / 文件上传 / 搜索框 / 富文本 / API 接口
- [ ] 根据功能类型制定攻击优先级清单

**12. 阶段⑤ 针对性渗透测试**
- [ ] 登录表单 → 调用 sqlmap MCP 进行 SQL 注入检测
- [ ] 文件上传接口 → 调用自定义上传绕过 MCP（Payload 投递 + 响应分析）
- [ ] 搜索/输入框 → 调用 XSS 检测 MCP（xsstrike 或自定义 Payload 投递）
- [ ] API 接口 → 调用 ffuf 进行参数 Fuzz
- [ ] 登录后自动携带 Cookie 深度测试已认证接口

**13. 自动渗透报告生成**
- [ ] 主脑 LLM 汇总所有阶段结果
- [ ] 输出：Markdown 格式渗透报告（风险分级 / 漏洞描述 / PoC 请求 / 修复建议）
- [ ] 前端渲染报告预览并支持导出

**14. 实时攻击态势大屏**
- [ ] 在「综合分析」页展示本次任务的动态进攻路径图
- [ ] 高亮已发现的漏洞节点，连接到对应功能点和目标

**15. RAG 知识库积累**
- [ ] 将历次扫描发现的漏洞写入向量数据库
- [ ] 主脑 LLM 分析新目标时可自动检索历史同类目标的漏洞经验
- [ ] 知识库支持手动录入红队经验（SQLi bypass / 上传绕过 Payload 等）

---

### 各功能栏独立能力保留计划

| 功能栏 | 全流程调用 | 独立操作 | 独立操作入口 |
|---|---|---|---|
| 端口扫描 | ✅ 阶段②自动调用 | ✅ 保留 | 信息收集 → 端口扫描（输入目标直接扫） |
| 子域名收集 | ✅ 阶段②自动调用 | ✅ 保留 | 信息收集 → 子域名收集（输入主域名） |
| 目录扫描 | ✅ 阶段②自动调用 | ✅ 保留 | 信息收集 → 目录扫描（输入URL） |
| 功能点检测 | ✅ 阶段④自动分析 | ✅ 保留 | 功能点检测页面（单独分析某URL） |
| 漏洞扫描任务 | ✅ 阶段⑤自动调用 | ✅ 保留 | 漏洞扫描 → 测试任务（手动选工具） |
| 安全报告 | ✅ 阶段⑥生成 | ❌ 需要数据 | 无法单独生成（依赖前置数据） |

---

*改造完成后预计完成度：**85%**。剩余 15% 为高级 PoC 利用链和大规模多目标并行。*
